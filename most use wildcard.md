𝐅𝐨𝐫  𝐌𝐂𝐐: specific step → 1,8,9,14,3

1.  ধাপ ১: ১ থেকে ৩০ পর্যন্ত প্রশ্ন নম্বর অটো-অ্যালাইন করা (Hanging Indent)
    পদ্ধতি ক: প্রশ্ন নম্বরের পর দাড়ি ও স্পেস থাকলে

Find what:

```text
([০-৯]{1,2})।[ ]{1,}
```

Replace with:

```text
\1।^t
```

2.  ধাপ ৮: ৪টি পৃথক লাইনে থাকা অপশনগুলোকে ২ লাইনে নিয়ে আসা (ক-খ এবং গ-ঘ)

Find what:

```text
(\(ক\)*)^13(\(খ\)*)^13(\(গ\)*)^13(\(ঘ\)*)
```

(নোট: ওয়াইল্ডকার্ড মোডে Find বক্সে এন্টার বা প্যারাগ্রাফ ব্রেক খুঁজতে ^13 লিখতে হয়।)
Replace with:

```text
\1^t\2^p\3^t\4
```

3.  ধাপ ৯: ৪টি অপশনকে ৪টি পৃথক লাইনে (নিচে নিচে) সাজানো

Find what:

```text
(\(ক\)*)(\(খ\)*)(\(গ\)*)(\(ঘ\)*)
```

Replace with:

```text
\1^p\2^p\3^p\4
```

4.  ধাপ ১৪: রোমান সংখ্যা ফরম্যাটিং
    পদ্ধতি ক: রোমান সংখ্যার পর হ্যাঙ্গিং ইনডেন্ট সেট করা
    যদি রোমান সংখ্যার পর ডট ও স্পেস থাকে (যেমন: i. ), তাহলে স্পেসকে ট্যাবে রূপান্তর করে হ্যাঙ্গিং ইনডেন্ট তৈরি করুন:

Find what:

```text
(<[ivx]{1,3}\.)[ ]
```

Replace with:

```text
\1^t
```

5.  ধাপ ৩: অপশনের ব্র্যাকেট মুছে নির্দিষ্ট ফন্ট সেট করা
    অপশনগুলোর চারপাশের ব্র্যাকেটগুলো যেমন: (ক) থেকে ক মুছে ফেলে ফন্ট (যেমন: NesarulOMR) সেট করার নিয়ম:
    Replace with বক্সের ভেতরের পুরো লেখাটি মুছে একদম খালি করুন।
    Find what:

```text
\(([কখগঘ])\)
```

ক খ গ ঘ এগুলোর পর যদি . থাকে তাহলে
Find What:

```text
([কখগঘ]).[ ]
```

Replace with:

```text
\1
```

7.  ভুল বশত left stop tab set করলে পুনরায় আবার set করার নিয়ম
    অব্যশই tab option থেকে 2 বা 3 সিলেক্ট করবেন বা ইচ্ছেমত পজিশন দিতে পারেন

Find What:

```text
(\(ক\)*)(\(খ\)*)(\(গ\)*)(\(ঘ\)*)
```

Replace:

```text
\1^t\2\3^t\4
```

𝐅𝐨𝐫  𝐂𝐐:

1.  ধাপ ১: ১ থেকে ৩০ পর্যন্ত প্রশ্ন নম্বর অটো-অ্যালাইন করা (Hanging Indent)
    পদ্ধতি ক: প্রশ্ন নম্বরের পর দাড়ি ও স্পেস থাকলে
    Find what:

```text
([০-৯]{1,2})।[ ]{1,}
```

Replace with:

```text
\1।^t
```

2.  ধাপ ৩: অপশনের ব্র্যাকেট মুছে নির্দিষ্ট ফন্ট সেট করা
    Find what:

```text
\(([কখগঘ])\)
```

Replace with:

```text
\1
```

3.  ধাপ ৬: শেষের পূর্ণমান নম্বরগুলোকে ডানে পাঠানো (Right Tab)
    প্রশ্নের শেষে থাকা নম্বরগুলোর (যেমন: ১, ২, ৩, ৪ বা ৫) আগের স্পেসকে ট্যাবে রূপান্তর করে ডানে সরানোর নিয়ম:
    Find what:

```text
([\?।])[ ]{1,}([১২৩৪৫৬৭৮৯০]{1,2})
```

Replace with:

```text
\1^t\2
```
